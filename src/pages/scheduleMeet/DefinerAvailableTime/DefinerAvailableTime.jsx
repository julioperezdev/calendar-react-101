import React from "react";
import StandardCase from "../../standardCase/StandardCase";

/**
 *
 * adding (object/argument) to StandardCase Component
 * to identify witch logic should exec (student or teacher)
 * store in memory data that will be use to consume by the student
 * and later the student can be booking a meet with the teacher in available time
 * later, the student can be a message with the information meet
 * and the teacher too
 */

const DefinerAvailableTime = () => {
    return (<div>
        <h1>TEACHER DASHBOARD</h1>
        <StandardCase/></div>)
}

export default DefinerAvailableTime